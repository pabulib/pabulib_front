# Nginx configuration for HTTP to HTTPS redirect
# This handles HTTP redirects efficiently at the web server level

server {
    listen 80;
    server_name pabulib.org;
    
    # Security headers
    add_header X-Frame-Options DENY always;
    add_header X-Content-Type-Options nosniff always;
    
    # Redirect all HTTP traffic to HTTPS
    return 301 https://$server_name$request_uri;
}

# Optional: If you want to use Nginx as reverse proxy (more robust)
# Uncomment this section and modify Docker setup accordingly
#
# server {
#     listen 443 ssl http2;
#     server_name pabulib.org;
#     
#     # SSL Configuration
#     ssl_certificate /etc/letsencrypt/live/pabulib.org/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/pabulib.org/privkey.pem;
#     ssl_session_timeout 1d;
#     ssl_session_cache shared:SSL:50m;
#     ssl_stapling on;
#     ssl_stapling_verify on;
#     
#     # Security headers
#     add_header Strict-Transport-Security "max-age=31536000" always;
#     add_header X-Frame-Options DENY always;
#     add_header X-Content-Type-Options nosniff always;
#     
#     # Proxy to Gunicorn
#     location / {
#         proxy_pass http://127.0.0.1:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_connect_timeout 60s;
#         proxy_send_timeout 60s;
#         proxy_read_timeout 60s;
#     }
# }
