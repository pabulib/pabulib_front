###########################################
# Production server environment variables #
###########################################

# Environment (set to "production" for HTTPS redirect)
ENVIRONMENT=production

# Admin user (for /admin routes)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your-secure-password-here

# Flask/app
FLASK_DEBUG=0
SECRET_KEY=your-production-secret-key-here

# Database configuration
MYSQL_ROOT_PASSWORD=your-root-password
MYSQL_DATABASE=pabulib
MYSQL_USER=pabulib_user
MYSQL_PASSWORD=your-db-password
MYSQL_PORT=3306

# PB files location (host paths)
# These are resolved relative to the repository root when docker-compose mounts them.
PB_FILES_DIR=./pb_files
PB_FILES_DEPRECIATED_DIR=./pb_files_depreciated

# Optional: Adminer (set port to run, comment out to disable)
# ADMINER_PORT=8080

# DB refresh settings
REFRESH_FULL=0
WAIT_FOR_DB_MAX_TRIES=120
WAIT_FOR_DB_SLEEP=3
WAIT_FOR_DB_GRACE=2

###########################################
# Analytics                               #
###########################################

# Google Analytics 4 Measurement ID (format: G-XXXXXXXXXX)
# Leave empty to disable analytics
GOOGLE_ANALYTICS_ID=

###########################################
# Error Tracking (Sentry)                #
###########################################

# Sentry Data Source Name (DSN) - get this from your Sentry project settings
# REQUIRED for production error tracking
SENTRY_DSN=https://your-dsn@sentry.io/your-project-id

# Sentry environment
SENTRY_ENVIRONMENT=production

# Performance monitoring sample rate (0.0 to 1.0)
# Use lower values in production (0.01 = 1%, 0.05 = 5%)
SENTRY_TRACES_SAMPLE_RATE=0.01

# Profiling sample rate (0.0 to 1.0)
# Use lower values in production (0.01 = 1%, 0.05 = 5%)
SENTRY_PROFILES_SAMPLE_RATE=0.01

# Release version for better error tracking (recommended for production)
# Format: version number, git SHA, or deployment identifier
SENTRY_RELEASE=v1.0.0
